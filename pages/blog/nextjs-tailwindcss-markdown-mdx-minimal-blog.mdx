export const metadata = {
  title: "Minimal blog with nextjs, tailwindcss and markdown!",
  date: "2021-04-04",
};

<article className="prose py-12 px-6 mx-auto dark:prose-dark">

# Minimal blog with nextjs, tailwindcss and markdown (MDX)

I decided to do a little blog on my personal site. As the first article, this is how I build it.

## Tools

[Next.js](https://nextjs.org/) is my favorite tools for making websites.
With a concept of zero configuration, with a lots of features (including hybrid static, server rendering, routing system, etc.) it letting you focus exclusively on your product and building it in a pleasant way.
[Tailwindcss](https://tailwindcss.com/) is a utility-first CSS framework for rapidly building styles.
[MDX](https://mdxjs.com/) is "Markdown for the component era". MDX let you write JSX in your Markdown documents.

For this article, I will assume you are already familiar with JavaScript and React.

## Project setup

```js live=true
var s = "JavaScript syntax highlighting";
alert(s);
```

```js
const TestComponent = () => {
  return <div>Test</div>;
};
```

command line pour dev

structure

tailwind css

why markdown
alternative cms (prismic, etc)

rendering markdown
il y a différentes options pour travailler le mdx avec nextjs. Je vais utiliser pour ma part @next/mdx.

tailwind css + markdown
tailwind a sorti un plugin https://github.com/tailwindlabs/tailwindcss-typography/blob/master/README.md
super simple on rajoute className="prose" et on embarque le style tailwind

homepage
comment get l'ensemble des blogs posts, explication rapide de fs, path.

random links
https://whoisryosuke.com/blog/2020/using-mdx-with-nextjs/
https://sergiodxa.com/articles/use-tailwindcss-typography-with-dark-mode-styles
https://sarim.work/blog/dynamic-imports-mdx

</article>
